{% extends "front/layout/base.html.twig" %}

{% block title %}Accueil {{ parent() }}{% endblock %}
{% block content %}
    <section id="homepage_carousel" class="carousel slide" data-ride="carousel">
        <div class="carousel-inner">
            {% for image in sliderImages %}
               {% if loop.index == 1 %}
                    <div class="carousel-item active">
                        <img class="d-block w-100" src="{{ asset('images/uploads/' ~ image.filename) }}" alt="{{ image.alt }}">
                    </div>
                   {% else %}
                       <div class="carousel-item">
                           <img class="d-block w-100" src="{{ asset('images/uploads/' ~ image.filename) }}" alt="{{ image.alt }}">
                       </div>
                {% endif %}
            {% endfor %}
        </div>

        <a class="carousel-control-prev" href="#homepage_carousel" role="button" data-slide="prev">
        </a>
        <a class="carousel-control-next" href="#homepage_carousel" role="button" data-slide="next">
        </a>
    </section>

    <section id="homepage_social">
        <div class="row text-center justify-content-center">
            <div class="col-12 col-md-6">
                <div class="row">
                    <div class="col-4 py-5">
                        <a href="{{ facebook_link }}"><i class="fa fa-facebook-square fa-5x text-pink"></i></a>
                    </div>
                    <div class="col-4  py-5">
                        <a href="{{ instagram_link }}"><i class="fa fa-instagram fa-5x text-pink"></i></a>
                    </div>
                    <div class="col-4  py-5">
                        <a href="{{ path('contact') }}"><i class="fa fa-user-plus fa-5x text-pink"></i></a>
                    </div>
                </div>
            </div>

        </div>
    </section>

    <section id="homepage_youtube">
        <div class="py-3 bg-black text-white text-center">
            <h2>On écoute quoi chez AMM en ce moment ?</h2>
        </div>

        <div class="d-flex justify-content-center bg-black">
            <iframe class="my-3" width="700" height="455" src="https://www.youtube.com/embed/h5XKTIZ6nUM" frameborder="0" allow="autoplay; encrypted-media" allowfullscreen></iframe>
        </div>
    </section>




{% endblock %}

